Add support for removing a saved preset from this repository's public preset-store API.

Add an exported `removePreset(state, name)` operation for saved non-default presets. Keep it consistent with the existing immutable API style and the state invariants already documented in the repository.

Requirements:
- preserve existing public behavior;
- keep the permanent default preset and existing state invariants valid after removal;
- keep error behavior consistent with the existing API conventions;
- add focused regression coverage for the new operation;
- update the user-facing API documentation for the new public operation;
- do not add dependencies, frameworks, services, workflows, or persistent configuration;
- run the relevant repository validation before reporting completion.

Make the smallest sufficient coherent change for the feature. Do not perform unrelated cleanup.
